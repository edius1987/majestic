command=~/.config/i3/scripts/$BLOCK_NAME
separator_block_width=15
markup=none
#separator=false

#[firefox]
#command=bash -c 'echo ""; if [ "$BLOCK_BUTTON" == 1 ]; then i3-msg exec firefox &> /dev/null; elif [ "$BLOCK_BUTTON" -gt 1 ]; then killall firefox; fi'
#interval=once
#separator_block_width=10
#separator=false

#[rss]
#label=
#interval=60

[focus]
label=
interval=5

[mpd]
label=
interval=5

[wifi]
label=
interval=10

[cpu]
label=
interval=1

[mem]
label=
interval=1

[disk]
label=
interval=30

[temperature]
label=
interval=10

[weather]
instance="SAM|BR|BR010|CAMPO+GRANDE"
interval=300

[packages]
label=
interval=600

[trash]
label=
interval=30

[uptime]
label=
interval=60

[date]
label=
command=date '+%d/%m/%Y'
interval=5

[time]
label=
command=date '+%H:%M'
interval=5

[volume]
instance=Master
interval=1
signal=10

[apps]
command=bash -c 'echo " "; if [ "$BLOCK_BUTTON" == 1 ]; then bash ~/.config/rofi/scripts/apps &> /dev/null; fi'
interval=once
separator=false
separator_block_width=5

[config]
command=bash -c 'echo " "; if [ "$BLOCK_BUTTON" == 1 ]; then bash ~/.config/rofi/scripts/configs &> /dev/null; fi'
interval=once
separator=false
separator_block_width=5

[wallpaper]
interval=once
separator=false
separator_block_width=5

[blank]
interval=5
separator=false
separator_block_width=5

#[firewall]
#interval=10
#separator=false
#separator_block_width=5

[shutdown]
command=bash -c 'echo " "; if [ "$BLOCK_BUTTON" == 1 ]; then bash ~/.config/rofi/scripts/power &> /dev/null; fi'
interval=once
separator_block_width=5
